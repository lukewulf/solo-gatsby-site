import * as React from 'react'
import Layout from '../../components/layout'

const ProjectPage = () => {
    return (
        <Layout pagetTitle="Project:XX">
            <p>This is a hardcoded project description</p>
        </Layout>
    )
}